{
    "version": 2,
    "buildCommand": "rm -rf dist && npm install && npm run build",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/app.js"
        }
    ]
}
